        <div class="show">
        <div class="show_info">
        <p class="posts_all">タイトル：<%= @post.title %></p>
           
              <p class="show_picture"><%= image_tag @post.image, size:"500x350" %></p>
             
        <p class="posts_all">魚の種類：<%= @post.aimble_fish%>
        <p class="posts_all">おすすめの時間帯：<%= @post.time_zone.name%>
        <p class="posts_all">おすすめの仕掛け：<%= @post.trick%>
        <p class="posts_all">ポイントの説明：<%= @post.comment%>
        <p class="posts_all">投稿者：<%=@post.user.nickname%>さん</p>
        </div>

        <%if user_signed_in?%>
        <div class= "positon">
        <%= link_to 'チャットルーム', post_messages_path(@post), class:"show_chatroom" %>
        <% if current_user == @post.user %>
        <%= link_to '編集', edit_post_path, class:"show_edit" %>
        <%= link_to '削除', post_path, method: :delete, class:"show_edit" %>
        <% end %>
        </div>
        <%end%>
        </div>